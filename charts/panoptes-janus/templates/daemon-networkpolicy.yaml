{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "panoptes-janus.fullname" . }}-daemon
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "panoptes-janus.daemon.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "panoptes-janus.daemon.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # gRPC from controller
    - ports:
        - port: {{ .Values.service.port }}
          protocol: TCP
      from:
        - podSelector:
            matchLabels:
              {{- include "panoptes-janus.controller.selectorLabels" . | nindent 14 }}
    # Prometheus metrics scraping
    - ports:
        - port: {{ .Values.observability.prometheus.port }}
          protocol: TCP
  egress:
    # DNS resolution
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
{{- end }}
