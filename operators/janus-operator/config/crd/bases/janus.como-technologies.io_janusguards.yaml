---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: janusguards.janus.como-technologies.io
spec:
  group: janus.como-technologies.io
  names:
    categories:
    - all
    - janus
    - security
    kind: JanusGuard
    listKind: JanusGuardList
    plural: janusguards
    shortNames:
    - jg
    singular: janusguard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Number of pods matching selector
      jsonPath: .status.observablePods
      name: Observable
      type: integer
    - description: Number of pods being guarded
      jsonPath: .status.guardedPods
      name: Guarded
      type: integer
    - description: Total denied access attempts
      jsonPath: .status.totalDeniedEvents
      name: Denied
      type: integer
    - description: Whether denials are enforced
      jsonPath: .spec.enforcing
      name: Enforcing
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: v1 is deprecated, migrate to v2
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          JanusGuard is the Schema for the janusguards API.
          It defines file access auditing and control rules for pods matching a selector.

          Deprecated: v1 is the legacy API version. Use v2 for new deployments.
          v1 is maintained for backward compatibility during migration only.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of JanusGuard
            properties:
              containerRuntime:
                allOf:
                - enum:
                  - containerd
                  - cri-o
                  - auto
                - enum:
                  - containerd
                  - cri-o
                  - auto
                default: auto
                description: |-
                  containerRuntime specifies which container runtime to use for PID detection.
                  Use "auto" to automatically detect the runtime.
                type: string
              enforcing:
                default: true
                description: |-
                  enforcing indicates whether access denials are enforced.
                  When false, denials are logged but access is allowed (dry-run mode).
                type: boolean
              logFormat:
                description: |-
                  logFormat is the custom log format template for events.
                  Supports Go template syntax with access to event fields.
                maxLength: 1024
                type: string
              paused:
                description: |-
                  paused indicates whether guarding is paused for this resource.
                  When true, no access control is enforced but existing guards are maintained.
                type: boolean
              selector:
                description: |-
                  selector is the label selector for pods to guard.
                  Only pods matching this selector will have access control applied.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subjects:
                description: |-
                  subjects is the list of access control rules to apply.
                  Each subject defines allow/deny rules for file access.
                items:
                  description: JanusGuardSubject defines file access control rules
                  properties:
                    allow:
                      description: |-
                        allow is the list of paths to explicitly allow access to.
                        Paths support glob patterns (e.g., "/app/**", "/etc/*.conf").
                      items:
                        type: string
                      maxItems: 100
                      type: array
                    audit:
                      description: |-
                        audit enables kernel audit log integration.
                        When true, events are also written to the kernel audit log.
                      type: boolean
                    autoAllowOwner:
                      description: |-
                        autoAllowOwner automatically allows access for the file owner.
                        Useful for applications that need to access their own files.
                      type: boolean
                    defaultResponse:
                      allOf:
                      - enum:
                        - allow
                        - deny
                        - audit
                      - enum:
                        - allow
                        - deny
                        - audit
                      default: audit
                      description: defaultResponse is the action when no allow/deny
                        rule matches.
                      type: string
                    deny:
                      description: |-
                        deny is the list of paths to explicitly deny access to.
                        Deny rules take precedence over allow rules.
                      items:
                        type: string
                      maxItems: 100
                      type: array
                    events:
                      description: |-
                        events is the list of fanotify event types to monitor.
                        Use "all" to monitor all event types.
                      items:
                        description: JanusEvent represents a fanotify event type for
                          file access auditing
                        enum:
                        - access
                        - open
                        - execute
                        - close
                        - all
                        type: string
                      minItems: 1
                      type: array
                    onlyDir:
                      description: onlyDir restricts monitoring to directories only.
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      description: |-
                        tags are custom key-value metadata attached to events from this subject.
                        Useful for categorization, compliance tracking, or alert routing.
                      maxProperties: 20
                      type: object
                  required:
                  - events
                  type: object
                maxItems: 20
                minItems: 1
                type: array
            required:
            - selector
            - subjects
            type: object
          status:
            description: status defines the observed state of JanusGuard
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the JanusGuard resource.
                  Standard condition types: Available, Progressing, Degraded
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              guardedPods:
                description: |-
                  guardedPods is the number of pods currently being guarded.
                  This should eventually equal observablePods when fully reconciled.
                format: int32
                type: integer
              lastReconcileTime:
                description: lastReconcileTime is when the controller last reconciled
                  this resource.
                format: date-time
                type: string
              observablePods:
                description: |-
                  observablePods is the number of pods matching the selector.
                  This is the total number of pods that could potentially be guarded.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed by the controller.
                  Used to determine if the status is up-to-date with the spec.
                format: int64
                type: integer
              podStatuses:
                description: podStatuses contains detailed status for each guarded
                  pod.
                items:
                  description: GuardedPodStatus represents the guard status for a
                    single pod
                  properties:
                    allowedCount:
                      description: allowedCount is the number of allowed access events
                        for this pod
                      format: int64
                      type: integer
                    deniedCount:
                      description: deniedCount is the number of denied access attempts
                        for this pod
                      format: int64
                      type: integer
                    lastDenialTime:
                      description: lastDenialTime is when the last access denial occurred
                      format: date-time
                      type: string
                    name:
                      description: name is the name of the pod being guarded
                      type: string
                    namespace:
                      description: namespace is the namespace of the pod
                      type: string
                    nodeName:
                      description: nodeName is the node where the pod is running
                      type: string
                  required:
                  - allowedCount
                  - deniedCount
                  - name
                  - namespace
                  - nodeName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              totalAllowedEvents:
                description: totalAllowedEvents is the total count of allowed access
                  events since creation.
                format: int64
                type: integer
              totalAuditedEvents:
                description: totalAuditedEvents is the total count of audit-only events
                  since creation.
                format: int64
                type: integer
              totalDeniedEvents:
                description: totalDeniedEvents is the total count of denied access
                  attempts since creation.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - description: Number of pods matching selector
      jsonPath: .status.observablePods
      name: Observable
      type: integer
    - description: Number of pods being guarded
      jsonPath: .status.guardedPods
      name: Guarded
      type: integer
    - description: Total denied access attempts
      jsonPath: .status.totalDeniedEvents
      name: Denied
      type: integer
    - description: All marks registered
      jsonPath: .status.marksRegistered
      name: Ready
      type: boolean
    - description: Whether denials are enforced
      jsonPath: .spec.enforcing
      name: Enforcing
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: "JanusGuard is the Schema for the janusguards API (short name:
          jg).\nIt defines file access auditing and enforcement rules using Linux
          fanotify for pods\nmatching a label selector. The janus-operator watches
          JanusGuard resources and instructs\nthe janusd daemon on each node to create
          kernel-level access control marks.\n\nUnlike ArgusWatcher (which detects
          changes), JanusGuard can actively block file access\nwhen spec.enforcing
          is true. This makes it suitable for runtime protection scenarios\nlike blocking
          container runtime socket access or protecting sensitive credentials.\n\nRequires:
          janusd DaemonSet with CAP_SYS_ADMIN (fanotify), CAP_SYS_PTRACE, CAP_DAC_READ_SEARCH.\nKernel
          requirements: Linux 5.x+ with fanotify support.\n\nQuick example:\n\n\tkubectl
          apply -f - <<EOF\n\tapiVersion: janus.panoptes.io/v2\n\tkind: JanusGuard\n\tmetadata:\n\t
          \ name: block-runtime-sockets\n\tspec:\n\t  enforcing: true\n\t  selector:\n\t
          \   matchLabels:\n\t      app: my-app\n\t  subjects:\n\t    - deny: [\"/var/run/docker.sock\",
          \"/run/containerd/containerd.sock\"]\n\t      events: [open, access]\n\t
          \     tags:\n\t        severity: critical\n\t        compliance: cis-kubernetes\n\tEOF"
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of JanusGuard
            properties:
              containerRuntime:
                allOf:
                - enum:
                  - containerd
                  - cri-o
                  - auto
                - enum:
                  - containerd
                  - cri-o
                  - auto
                default: auto
                description: |-
                  containerRuntime specifies which container runtime to use for PID detection.
                  "auto" probes for containerd and CRI-O sockets in standard locations.
                  Set explicitly if auto-detection fails or if using a non-standard socket path.
                type: string
              enforcing:
                default: true
                description: |-
                  enforcing controls whether deny rules actively block file access.
                  When true (default), denied paths return EACCES to the accessing process.
                  When false (dry-run mode), denials are logged but access is permitted.
                  IMPORTANT: Start with enforcing=false in production to validate rules
                  before enabling enforcement. See docs/guides/enabling-enforcement.md.
                type: boolean
              logFormat:
                description: |-
                  logFormat is a custom Go template for formatting event log output.
                  Available fields: .Event, .Path, .Response, .Timestamp, .Pod, .Node.
                  Leave empty to use the default structured JSON format.
                maxLength: 1024
                type: string
              paused:
                description: |-
                  paused temporarily suspends all access control for this resource.
                  When true, all fanotify marks are removed and no access control is applied.
                  Use this during planned maintenance windows or emergency response.
                  Set back to false to resume guarding.
                type: boolean
              selector:
                description: |-
                  selector is the label selector for pods to guard.
                  Only pods matching this selector will have fanotify access control applied.
                  Use matchLabels for simple key-value matching, or matchExpressions for
                  set-based requirements. Example: {"matchLabels": {"pci-dss/scope": "in-scope"}}.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subjects:
                description: |-
                  subjects is the list of access control rules to apply.
                  Each subject defines independent allow/deny rules for file access paths.
                  Use multiple subjects to apply different policies with different tags.
                items:
                  description: |-
                    JanusGuardSubject defines file access control rules for a set of paths.
                    Each subject evaluates deny rules first, then allow rules, then falls back to defaultResponse.
                    Multiple subjects can target different paths with independent rules and tags.
                  properties:
                    allow:
                      description: |-
                        allow is the list of paths to explicitly allow access to.
                        Paths support glob patterns (e.g., "/app/**", "/etc/*.conf").
                        Allow rules are evaluated after deny rules; if a path matches both, deny wins.
                      items:
                        type: string
                      maxItems: 100
                      type: array
                    audit:
                      description: |-
                        audit enables kernel audit log integration via AUDIT_WRITE.
                        When true, access events are written to the kernel audit log in addition
                        to being reported via gRPC. Useful for compliance frameworks requiring
                        kernel-level audit trails (NIST 800-53 AU-2).
                      type: boolean
                    autoAllowOwner:
                      description: |-
                        autoAllowOwner automatically allows access when the accessing process UID
                        matches the file owner UID. Useful for applications that need to read their
                        own config files or Kubernetes service account tokens.
                      type: boolean
                    defaultResponse:
                      allOf:
                      - enum:
                        - allow
                        - deny
                        - audit
                      - enum:
                        - allow
                        - deny
                        - audit
                      default: audit
                      description: |-
                        defaultResponse is the action taken when a file access matches no allow or deny rule.
                        "audit" (default) logs without affecting access. "allow" silently permits.
                        "deny" blocks access (only when spec.enforcing is true).
                      type: string
                    deny:
                      description: |-
                        deny is the list of paths to block access to.
                        Paths support glob patterns (e.g., "/etc/shadow", "/var/run/docker.sock").
                        Deny rules take precedence over allow rules. When spec.enforcing is true,
                        access is blocked with EACCES. When false, access is logged but permitted.
                      items:
                        type: string
                      maxItems: 100
                      type: array
                    events:
                      description: |-
                        events is the list of fanotify event types to monitor.
                        Use "all" to monitor all event types. For access control, "open" and "access"
                        are the most common. "execute" requires kernel 5.0+.
                      items:
                        description: |-
                          JanusEvent represents a Linux fanotify event type for file access auditing.
                          These map to fanotify_mark(2) event flags. Unlike inotify, fanotify can
                          block access (when enforcing is true), not just detect it.
                        enum:
                        - access
                        - open
                        - execute
                        - close
                        - all
                        type: string
                      minItems: 1
                      type: array
                    onlyDir:
                      description: onlyDir restricts fanotify marks to directories
                        only.
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      description: |-
                        tags are custom key-value metadata attached to events from this subject.
                        Useful for compliance tracking (e.g., "compliance": "pci-dss", "severity": "critical"),
                        alert routing, or categorization in downstream SIEM systems.
                      maxProperties: 20
                      type: object
                  required:
                  - events
                  type: object
                maxItems: 20
                minItems: 1
                type: array
            required:
            - selector
            - subjects
            type: object
          status:
            description: status defines the observed state of JanusGuard
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the JanusGuard resource.
                  Standard condition types: Available, Progressing, Degraded
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              guardedPods:
                description: |-
                  guardedPods is the number of pods currently being guarded.
                  This should eventually equal observablePods when fully reconciled.
                format: int32
                type: integer
              lastReconcileTime:
                description: lastReconcileTime is when the controller last reconciled
                  this resource.
                format: date-time
                type: string
              marksRegistered:
                description: marksRegistered indicates all fanotify marks are registered.
                type: boolean
              observablePods:
                description: |-
                  observablePods is the number of pods matching the selector.
                  This is the total number of pods that could potentially be guarded.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed by the controller.
                  Used to determine if the status is up-to-date with the spec.
                format: int64
                type: integer
              podStatuses:
                description: podStatuses contains detailed status for each guarded
                  pod.
                items:
                  description: GuardedPodStatus represents the guard status for a
                    single pod
                  properties:
                    allowedCount:
                      description: allowedCount is the number of allowed access events
                        for this pod
                      format: int64
                      type: integer
                    deniedCount:
                      description: deniedCount is the number of denied access attempts
                        for this pod
                      format: int64
                      type: integer
                    lastDenialTime:
                      description: lastDenialTime is when the last access denial occurred
                      format: date-time
                      type: string
                    marksRegistered:
                      description: marksRegistered indicates fanotify marks are active
                        for this pod.
                      type: boolean
                    mountCount:
                      description: mountCount is the number of container mounts with
                        active marks.
                      format: int32
                      type: integer
                    name:
                      description: name is the name of the pod being guarded
                      type: string
                    namespace:
                      description: namespace is the namespace of the pod
                      type: string
                    nodeName:
                      description: nodeName is the node where the pod is running
                      type: string
                    readyAt:
                      description: readyAt is when guards became ready for this pod.
                      format: date-time
                      type: string
                  required:
                  - allowedCount
                  - deniedCount
                  - name
                  - namespace
                  - nodeName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              readyAt:
                description: readyAt is when the guard became fully ready (all marks
                  registered).
                format: date-time
                type: string
              totalAllowedEvents:
                description: totalAllowedEvents is the total count of allowed access
                  events since creation.
                format: int64
                type: integer
              totalAuditedEvents:
                description: totalAuditedEvents is the total count of audit-only events
                  since creation.
                format: int64
                type: integer
              totalDeniedEvents:
                description: totalDeniedEvents is the total count of denied access
                  attempts since creation.
                format: int64
                type: integer
              totalMountCount:
                description: totalMountCount is the total number of container mounts
                  being guarded.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
