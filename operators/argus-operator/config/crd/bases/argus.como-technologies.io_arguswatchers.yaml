---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: arguswatchers.argus.como-technologies.io
spec:
  group: argus.como-technologies.io
  names:
    categories:
    - all
    - argus
    - security
    kind: ArgusWatcher
    listKind: ArgusWatcherList
    plural: arguswatchers
    shortNames:
    - aw
    singular: arguswatcher
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Number of pods matching selector
      jsonPath: .status.observablePods
      name: Observable
      type: integer
    - description: Number of pods being watched
      jsonPath: .status.watchedPods
      name: Watched
      type: integer
    - description: Total events detected
      jsonPath: .status.eventsDetected
      name: Events
      type: integer
    - description: Whether watching is paused
      jsonPath: .spec.paused
      name: Paused
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: v1 is deprecated, migrate to v2
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ArgusWatcher is the Schema for the arguswatchers API.
          It defines file integrity monitoring rules for pods matching a selector.

          Deprecated: v1 is the legacy API version. Use v2 for new deployments.
          v1 is maintained for backward compatibility during migration only.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ArgusWatcher
            properties:
              containerRuntime:
                allOf:
                - enum:
                  - containerd
                  - cri-o
                  - auto
                - enum:
                  - containerd
                  - cri-o
                  - auto
                default: auto
                description: |-
                  containerRuntime specifies which container runtime to use for PID detection.
                  Use "auto" to automatically detect the runtime.
                type: string
              logFormat:
                description: |-
                  logFormat is the custom log format template for events.
                  Supports Go template syntax with access to event fields.
                maxLength: 1024
                type: string
              paused:
                description: |-
                  paused indicates whether watching is paused for this resource.
                  When true, no new watches will be created but existing ones are maintained.
                type: boolean
              selector:
                description: |-
                  selector is the label selector for pods to watch.
                  Only pods matching this selector will be monitored.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subjects:
                description: |-
                  subjects is the list of watch targets defining what to monitor.
                  Each subject specifies paths and events to watch.
                items:
                  description: ArgusWatcherSubject defines a watch target with paths
                    and events
                  properties:
                    events:
                      description: |-
                        events is the list of inotify events to watch for.
                        Use "all" to watch for all event types.
                      items:
                        description: ArgusEvent represents an inotify event type for
                          file integrity monitoring
                        enum:
                        - access
                        - attrib
                        - closewrite
                        - closenowrite
                        - close
                        - create
                        - delete
                        - deleteself
                        - modify
                        - moveself
                        - movedfrom
                        - movedto
                        - move
                        - open
                        - all
                        type: string
                      minItems: 1
                      type: array
                    followMove:
                      description: |-
                        followMove tracks moved files by inode.
                        When true, files are tracked across moves within the watched paths.
                      type: boolean
                    ignore:
                      description: |-
                        ignore is the list of glob patterns to ignore.
                        Patterns follow standard glob syntax (e.g., "*.tmp", "**/node_modules/**").
                      items:
                        type: string
                      maxItems: 50
                      type: array
                    maxDepth:
                      description: |-
                        maxDepth limits the recursion depth when recursive is enabled.
                        A value of 0 means unlimited depth. Only effective when recursive is true.
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                    onlyDir:
                      description: |-
                        onlyDir restricts watching to directories only.
                        File events in watched directories will still be reported.
                      type: boolean
                    paths:
                      description: |-
                        paths is the list of file or directory paths to monitor.
                        Paths are relative to the container's filesystem root.
                      items:
                        type: string
                      maxItems: 100
                      minItems: 1
                      type: array
                    recursive:
                      description: |-
                        recursive enables recursive directory watching.
                        When true, all subdirectories are also monitored.
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      description: |-
                        tags are custom key-value metadata attached to events from this subject.
                        Useful for categorization, compliance tracking, or alert routing.
                      maxProperties: 20
                      type: object
                  required:
                  - events
                  - paths
                  type: object
                maxItems: 20
                minItems: 1
                type: array
            required:
            - selector
            - subjects
            type: object
          status:
            description: status defines the observed state of ArgusWatcher
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ArgusWatcher resource.
                  Standard condition types: Available, Progressing, Degraded
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eventsDetected:
                description: eventsDetected is the total count of file events detected
                  since creation.
                format: int64
                type: integer
              lastReconcileTime:
                description: lastReconcileTime is when the controller last reconciled
                  this resource.
                format: date-time
                type: string
              observablePods:
                description: |-
                  observablePods is the number of pods matching the selector.
                  This is the total number of pods that could potentially be watched.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed by the controller.
                  Used to determine if the status is up-to-date with the spec.
                format: int64
                type: integer
              podStatuses:
                description: podStatuses contains detailed status for each watched
                  pod.
                items:
                  description: WatchedPodStatus represents the watch status for a
                    single pod
                  properties:
                    lastEventTime:
                      description: lastEventTime is when the last event was received
                        from this pod
                      format: date-time
                      type: string
                    name:
                      description: name is the name of the pod being watched
                      type: string
                    namespace:
                      description: namespace is the namespace of the pod
                      type: string
                    nodeName:
                      description: nodeName is the node where the pod is running
                      type: string
                    watchDescriptors:
                      description: watchDescriptors is the number of inotify watch
                        descriptors for this pod
                      format: int32
                      type: integer
                  required:
                  - name
                  - namespace
                  - nodeName
                  - watchDescriptors
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              totalWatchDescriptors:
                description: |-
                  totalWatchDescriptors is the total number of inotify watch descriptors in use.
                  High values may indicate the need to increase inotify limits.
                format: int32
                type: integer
              watchedPods:
                description: |-
                  watchedPods is the number of pods currently being watched.
                  This should eventually equal observablePods when fully reconciled.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - description: Number of pods matching selector
      jsonPath: .status.observablePods
      name: Observable
      type: integer
    - description: Number of pods being watched
      jsonPath: .status.watchedPods
      name: Watched
      type: integer
    - description: Total events detected
      jsonPath: .status.eventsDetected
      name: Events
      type: integer
    - description: All watches registered
      jsonPath: .status.watchesReady
      name: Ready
      type: boolean
    - description: Whether watching is paused
      jsonPath: .spec.paused
      name: Paused
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: |-
          ArgusWatcher is the Schema for the arguswatchers API.
          It defines file integrity monitoring rules for pods matching a selector.
          This is the v2 API - the current recommended version for new deployments.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ArgusWatcher
            properties:
              containerRuntime:
                allOf:
                - enum:
                  - containerd
                  - cri-o
                  - auto
                - enum:
                  - containerd
                  - cri-o
                  - auto
                default: auto
                description: |-
                  containerRuntime specifies which container runtime to use for PID detection.
                  Use "auto" to automatically detect the runtime.
                type: string
              logFormat:
                description: |-
                  logFormat is the custom log format template for events.
                  Supports Go template syntax with access to event fields.
                maxLength: 1024
                type: string
              paused:
                description: |-
                  paused indicates whether watching is paused for this resource.
                  When true, no new watches will be created but existing ones are maintained.
                type: boolean
              selector:
                description: |-
                  selector is the label selector for pods to watch.
                  Only pods matching this selector will be monitored.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subjects:
                description: |-
                  subjects is the list of watch targets defining what to monitor.
                  Each subject specifies paths and events to watch.
                items:
                  description: ArgusWatcherSubject defines a watch target with paths
                    and events
                  properties:
                    events:
                      description: |-
                        events is the list of inotify events to watch for.
                        Use "all" to watch for all event types.
                      items:
                        description: ArgusEvent represents an inotify event type for
                          file integrity monitoring
                        enum:
                        - access
                        - attrib
                        - closewrite
                        - closenowrite
                        - close
                        - create
                        - delete
                        - deleteself
                        - modify
                        - moveself
                        - movedfrom
                        - movedto
                        - move
                        - open
                        - all
                        type: string
                      minItems: 1
                      type: array
                    followMove:
                      description: |-
                        followMove tracks moved files by inode.
                        When true, files are tracked across moves within the watched paths.
                      type: boolean
                    ignore:
                      description: |-
                        ignore is the list of glob patterns to ignore.
                        Patterns follow standard glob syntax (e.g., "*.tmp", "**/node_modules/**").
                      items:
                        type: string
                      maxItems: 50
                      type: array
                    maxDepth:
                      description: |-
                        maxDepth limits the recursion depth when recursive is enabled.
                        A value of 0 means unlimited depth. Only effective when recursive is true.
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                    onlyDir:
                      description: |-
                        onlyDir restricts watching to directories only.
                        File events in watched directories will still be reported.
                      type: boolean
                    paths:
                      description: |-
                        paths is the list of file or directory paths to monitor.
                        Paths are relative to the container's filesystem root.
                      items:
                        type: string
                      maxItems: 100
                      minItems: 1
                      type: array
                    recursive:
                      description: |-
                        recursive enables recursive directory watching.
                        When true, all subdirectories are also monitored.
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      description: |-
                        tags are custom key-value metadata attached to events from this subject.
                        Useful for categorization, compliance tracking, or alert routing.
                      maxProperties: 20
                      type: object
                  required:
                  - events
                  - paths
                  type: object
                maxItems: 20
                minItems: 1
                type: array
            required:
            - selector
            - subjects
            type: object
          status:
            description: status defines the observed state of ArgusWatcher
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ArgusWatcher resource.
                  Standard condition types: Available, Progressing, Degraded
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eventsDetected:
                description: eventsDetected is the total count of file events detected
                  since creation.
                format: int64
                type: integer
              lastReconcileTime:
                description: lastReconcileTime is when the controller last reconciled
                  this resource.
                format: date-time
                type: string
              observablePods:
                description: |-
                  observablePods is the number of pods matching the selector.
                  This is the total number of pods that could potentially be watched.
                format: int32
                type: integer
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed by the controller.
                  Used to determine if the status is up-to-date with the spec.
                format: int64
                type: integer
              podStatuses:
                description: podStatuses contains detailed status for each watched
                  pod.
                items:
                  description: WatchedPodStatus represents the watch status for a
                    single pod
                  properties:
                    lastEventTime:
                      description: lastEventTime is when the last event was received
                        from this pod
                      format: date-time
                      type: string
                    name:
                      description: name is the name of the pod being watched
                      type: string
                    namespace:
                      description: namespace is the namespace of the pod
                      type: string
                    nodeName:
                      description: nodeName is the node where the pod is running
                      type: string
                    readyAt:
                      description: readyAt is when watches became ready for this pod.
                      format: date-time
                      type: string
                    watchDescriptors:
                      description: watchDescriptors is the number of inotify watch
                        descriptors for this pod
                      format: int32
                      type: integer
                    watchesReady:
                      description: watchesReady indicates watches are active for this
                        pod.
                      type: boolean
                  required:
                  - name
                  - namespace
                  - nodeName
                  - watchDescriptors
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              readyAt:
                description: readyAt is when the watcher became fully ready (all watches
                  registered).
                format: date-time
                type: string
              totalWatchDescriptors:
                description: |-
                  totalWatchDescriptors is the total number of inotify watch descriptors in use.
                  High values may indicate the need to increase inotify limits.
                format: int32
                type: integer
              watchedPods:
                description: |-
                  watchedPods is the number of pods currently being watched.
                  This should eventually equal observablePods when fully reconciled.
                format: int32
                type: integer
              watchesReady:
                description: watchesReady indicates all inotify watches are registered
                  and active.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
